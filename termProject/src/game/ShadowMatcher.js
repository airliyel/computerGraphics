// Shadow Match system
// if shadow is matched, then clear the stage and go to clear scene
// shadow matching is based on the shape of the shadow, and the shape of the target shadow object

const ShadowMatcher = {
    
};

export default ShadowMatcher;